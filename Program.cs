Console.Write("Bir sayı girin: ");
int sayi = Convert.ToInt32(Console.ReadLine());

int toplam = 0;
int sayı1 = sayi;

if (toplam == sayı1)
{
    Console.WriteLine(" bir Armstrong sayısıdır.");
}
else
{
    Console.WriteLine("bir Armstrong sayısı değildir.");
}

Console.ReadKey();